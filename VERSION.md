# Playback Cifras IA — VERSION

Versão atual: `v2.3.1`

Linha de desenvolvimento: `develop-react`

Branch da versão:

```text
feature/v2-3-1-react-runtime-validation
```

## v2.3.1 — React Runtime Validation

Validação técnica da arquitetura React/Vite antes da integração com Google Drive.

Inclui:

- Workflow `React Build Validation`.
- Validação automática com `npm install`.
- Validação automática com `npm run build`.
- Script `npm run validate`.
- Arquivo `public/config.js` seguro para runtime inicial.
- `.gitignore` para arquivos locais e artefatos de build.

## Fora do escopo desta versão

- Google Drive final.
- Repertórios.
- Favoritos.
- IA.
- Sincronização PDF ↔ áudio.
